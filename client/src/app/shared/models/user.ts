export type User = {
  firstName: string;
  lastName: string;
  email: string;
  address: string;
};

export type Adress = {
  line1: string;
  line2?: string;
  city: string;
  state: string;
  count: string;
  postalCode: string;
};
